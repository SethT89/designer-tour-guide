name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      # Split from postinstall so a copy-script failure is distinct from an install failure.
      - run: npm ci --ignore-scripts
      - run: node scripts/copy-maplibre-worker.mjs
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test:run
      - run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder-anon-key
          NEXT_PUBLIC_MAP_CENTER_LNG: "-96.7970"
          NEXT_PUBLIC_MAP_CENTER_LAT: "32.7767"
          NEXT_PUBLIC_MAP_ZOOM: "11"
          NEXT_PUBLIC_DEFAULT_CITY: Dallas
